const MediumCard = ({ title, content }) => (
  <div className="medium-card">
    <h3 className="medium-card__title">{title}</h3>
    <p className="medium-card__content">{content}</p>
  </div>
);

export default MediumCard;
